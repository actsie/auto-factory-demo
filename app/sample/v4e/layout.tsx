export const metadata = {
    title: "V4E · Voice for Everything",
    description: "AI voice agents for every business conversation.",
};

export default function V4ELayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
